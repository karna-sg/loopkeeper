node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Secrets — never commit
.env
.env.*
# ...but DO ship the example templates contributors need
!.env.example
!**/.env.example
deploy/.env
deploy/loopkeeper.env
*.pem
*.p8
AuthKey_*.p8

# Databases, encrypted vault & keys (defense-in-depth — these default to the
# data volume / ~/.loopkeeper, not the repo, but never commit one by accident)
*.db
*.db-wal
*.db-shm
*.sqlite
*.enc
master.key
*.mobileprovision

# Local run artifacts
*.local.json
coverage/

# Phase 2 engineering worker — never commit
deploy/deploy-key
deploy/deploy-key.pub
deploy/gh-app.pem
agent-logs/
worktrees/
**/.claude/

# Real-inbox scratch fixtures (personal data — never commit)
.scratch/

# iOS — XcodeGen regenerates the project from project.yml; Xcode user state is per-machine
ios/Loopkeeper.xcodeproj/
ios/DerivedData/
**/xcuserdata/
*.xcuserstate
*.ipa
